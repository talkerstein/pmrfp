# PMRFP Handoff — 2026-06-05 (go-live in progress)

> Resume from this in a fresh chat. We are mid-launch. Code is done; the
> remaining work is mostly browser/DNS/Stripe config that only Rishon can do.

---

## ▶ RESUME HERE — exact steps left to launch (in order)

1. **Confirm `STRIPE_SECRET_KEY` in Vercel = the NEWLY-ROLLED key.** Rishon pasted live `sk_live` keys in chat (exposed). Roll the secret key in Stripe (Developers → API keys → roll), then make sure Vercel's `STRIPE_SECRET_KEY` holds the new one.
2. **Merge the `geo-launch` PR** → `https://github.com/talkerstein/pmrfp/pull/new/geo-launch`. This deploys this week's code **and** activates the live Stripe env vars in one Vercel deploy. *(Do NOT just "Redeploy" old master.)*
3. **Smoke-test `pmrfp.vercel.app`** (homepage, /sign-up, /pricing) once deployed.
4. **$1 live checkout test** (real card) → confirm RFP access unlocks + the Stripe webhook fires + a paid org appears. Refund the test charge yourself.
5. **Apply Supabase migrations** to prod (SQL editor): the geo migration `supabase/migrations/20260604000001_region_liquidity_waitlist_notifs.sql` + REA-role + expired-status (from `briefs/monetize-checklist.html`). Until then, founding-region features stay dormant (safe) and REA signups error.
6. **Resend / email:** verify `pmrfp.com` in Resend (add its DNS records at Hostinger + the DMARC — see `briefs/hostinger-dns-checklist.html`). Add **rotated** `RESEND_API_KEY` + `RESEND_FROM_EMAIL="PMRFP <info@pmrfp.com>"` to Vercel. Then I (Claude) fire a test email → confirm it lands in `info@`.
7. **Analytics:** GA4/PostHog + 7 events + Search Console verify (before driving traffic).
8. **DNS cutover (LAST)** in **Hostinger DNS Zone** (not GoDaddy): apex **A** + **www CNAME** → Vercel (🚨 **keep the Hostinger MX records**). Then set `NEXT_PUBLIC_SITE_URL` + Supabase Auth URLs → `https://pmrfp.com`, and update the Stripe webhook URL → `https://pmrfp.com/api/stripe/webhook`.
9. **Final end-to-end test on `pmrfp.com`.** → 🎉 LAUNCHED.
10. **First customers:** seed 3–5 GTA RFPs via admin so the board isn't empty; R. Talkar LinkedIn launch post + cold DMs (copy in `briefs/marketing-onepagers-chatgpt.md`).

---

## 🔴 SECURITY — secrets exposed in chat (ROTATE ALL)

Rishon pasted these in plaintext during the session — treat as compromised:
- **Hostinger email password** → change it.
- **Resend API key** (`re_…`) → regenerate in Resend; use the new one in Vercel.
- **Stripe live secret key(s)** (`sk_live_…`) → roll in Stripe; confirm Vercel has the new one.
- Stripe **publishable key** (`pk_live_…`) is public — fine, no action.

---

## ✅ What's DONE

- **13 commits** pushed to branch **`geo-launch`** (PR open, NOT merged). `master`/prod still runs old code.
- **Stripe: LIVE.** All 6 Vercel **Production** env vars set + verified:
  - `STRIPE_PRICE_TRADE_PRO_ANNUAL` = `price_1Tf6riDfrKup3VPaydn1EK8t`
  - `STRIPE_PRICE_TRADE_PRO_MONTHLY` = `price_1Tf6rhDfrKup3VParV47LK3l`
  - `STRIPE_PRICE_FEATURED_ANNUAL` = `price_1Tf6rnDfrKup3VPaaILo82uW`
  - `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` = `pk_live_51TcYkB…lnBSco`
  - `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` = set by Rishon (verify secret is the rolled one)
- **Stripe webhook** created → `https://pmrfp.vercel.app/api/stripe/webhook`, scope "Your account", 5 events: `checkout.session.completed`, `customer.subscription.{created,updated,deleted}`, `invoice.payment_failed`. (Switch URL to pmrfp.com after DNS.)
- **Stripe Pricing Table** (`prctbl_…`) exists but is **NOT used** — do not embed it (it bypasses the app's org-linked checkout → access wouldn't unlock).
- **`info@pmrfp.com`** Hostinger inbox exists (receiving works). App contact + admin-notify + Resend FROM all point to it.

## ⏳ What's PENDING
Merge PR · live checkout test · Supabase migrations · Resend domain verify + env vars · analytics · DNS cutover. (See RESUME steps above.)

---

## 🧭 Infra map (important)
- **Registrar:** GoDaddy (ownership only).
- **DNS host:** **Hostinger** (nameservers `dns-parking.com`). **All DNS records are edited in Hostinger**, NOT GoDaddy.
- **Website:** Vercel (`talkersteins-projects/pmrfp`), auto-deploys on push to `master`. Currently pmrfp.com → old WordPress (on Hostinger) until DNS cutover.
- **Email inbox:** Hostinger (`info@`, MX = mx1/mx2.hostinger.com). **App-sent email:** Resend (pending verify).
- **DB:** Supabase ref `tumexryzbzmtijvbloch`.

---

## 🗂️ Key decisions THIS session (all committed in geo-launch)
- **NO refunds.** Subscriptions non-refundable; cancel anytime, access to end of period. Removed the "30-day money-back guarantee" everywhere. (`briefs/referral-payout-policy.md` + site copy.)
- **GTA beachhead.** Funnel re-anchored: homepage "Now live in the GTA · your region next." Platform + SEO stay geography-neutral. (MBA seat's call; "challenge accepted.")
- **Geography guardrails (P0/P1).** Region-liquidity model + "founding-region" waitlist (capture demand in low-supply regions instead of empty board) + self-serve "add yourself anywhere." Migration not yet applied to prod.
- **Referral** removed from primary nav (footer + direct links only); payout = **$75 ~30 days after the referred trade's payment clears** (chargeback-safe), replacing the 90-day rule. Swept all copy.
- Stripped residual "Canada-first/GTA-first" from the core funnel; ~165 Canada refs remain in SEO pages (intentional / P4 sweep).
- **17-agent council** ran (avg 6.7/10): build was right, but **STOP building → ship go-live**; #1 gaps = thin liquidity + no trust proof + no analytics.

## 📁 Reference files (in `briefs/`)
- `handoff-2026-06-05.md` (this) · `deploy-explained.html` · `go-live-3-steps.html` · `hostinger-dns-checklist.html`
- `marketing-onepagers-chatgpt.md` · `onepager-trades.html` · `onepager-pms.html` · `referral-payout-policy.md` · `marketing-handoff.md`
- Plan: `~/.claude/plans/okay-can-i-ask-lovely-pelican.md`

---

## Resume prompt for a fresh chat
> Resume PMRFP launch — read `D:\Claude\pmrfp\briefs\handoff-2026-06-05.md` and continue from the "RESUME HERE" steps. We just finished setting all 6 Stripe env vars in Vercel; next is merging the geo-launch PR.
